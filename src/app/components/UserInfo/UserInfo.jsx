import React from 'react';
import ArrowIcon from '../Svg/ArrowIcon';

const UserInfo = () => (
  <button className="user-info" type="button">
    AndreyV
    <ArrowIcon className="user-info__icon" />
  </button>
);

export default UserInfo;
